# Roster Creator — Project Context

> Auto-generated summary for reuse in future conversations. Last updated: 2026-08-20 (UI redesign session).

## Stack

- **Frontend:** React + Vite + TypeScript SPA, Tailwind CSS. Lives in `frontend/`.
- **Backend:** Express + TypeScript + Prisma ORM + PostgreSQL. Lives in `backend/`.
- **Repo:** `D:\claude\projects\Roster_Creator`, GitHub `TaoXiong05/Roster_Creator`.
- Commit only when the user explicitly asks — do not commit proactively.

## What the app does

A staff rostering tool: manage staff, groups, shift templates, responsibility templates, and build/publish rosters (with AI-assisted generation, CSV/PDF/ICS export, email sending).

## i18n system (English default, Chinese toggle)

Custom-built, no external library.

- `frontend/src/i18n/en.ts` / `zh.ts` — parallel nested dictionaries, namespaced (`common`, `nav`, `auth.*`, `dashboard.cards.*`, `staff`, `groups`, `shiftTemplates`, `responsibilities`, `rosters`, `dayShiftDialog`, `unavailableDatesDialog`, `preferenceFields`, `timeInput`, `mascot`, `weekdaysShort` array).
- `frontend/src/i18n/LanguageContext.tsx`:
  - `translate(language, key, vars?)` — dot-path lookup + `{{var}}` interpolation, falls back to English dict, returns raw key if missing anywhere.
  - `getDictionary(language)` — full dict object, used for structured data like `weekdaysShort`.
  - `LanguageProvider` — Context provider, `language` state persisted to `localStorage` key `roster-creator-language`, defaults to `'en'`. Reads/writes wrapped in try/catch (handles jsdom tests and private-browsing).
  - `useLanguage()` — hook returning `{ language, setLanguage, t }`.
  - `LanguageContext` has a **non-throwing default value** (English) instead of `undefined`, so components work correctly even when rendered outside a `<LanguageProvider>` (e.g. tests that don't wrap render calls).
- `frontend/src/components/LanguageToggle.tsx` — single button showing the *other* language's name as its label; placed in `AppShell` (sidebar + mobile bar) for authenticated pages, and top-right of `AuthLayout` for pre-auth pages.
- Localized punctuation: `common.listSeparator` (en: `', '`, zh: `'、'`) and `common.colon` (en: `': '`, zh: `'：'`) — added after a grep sweep found hardcoded Chinese punctuation leaking into English strings. Always use these instead of literal `、`/`：`.
- Every page/component is translated; user-entered data (names, custom shift template names, etc.) is correctly left untouched.
- `frontend/src/pages/HelpPage.tsx` is the one exception to the generic `t()` lookup pattern — it defines its own `CONTENT: Record<'en'|'zh', HelpContent>` object since its content is large structured prose (steps/features/FAQs), not simple string keys.

## Date formatting — DD/MM/YYYY everywhere (Australian convention)

- `frontend/src/utils/date.ts` — `formatDate(value: string): string`, parses `YYYY-MM-DD` or ISO datetime, returns `DD/MM/YYYY`. Pure display formatter; native `<input type="date">` fields are left alone (browser-controlled).
- Backend CSV/PDF exports use a local `toDisplayDate()` helper in `backend/src/rosters/exportRoutes.ts`, applied only to CSV/PDF row mapping.
- **ICS export is deliberately excluded** — must stay in iCalendar's compact numeric format (`YYYYMMDDTHHMMSS`) for RFC 5545 compliance. Regression test asserts `DTSTART:20260817T060000` is unchanged.

## Time input — auto-colon + analog clock picker

`frontend/src/components/TimeInput.tsx` — combines:
1. Auto-colon text input: `autoFormatTime(raw)` strips non-digits (`raw.replace(/\D/g, '').slice(0,4)`), inserts `:` after 2 digits.
2. A clock-icon button opening an absolutely-positioned popover with a two-ring 24-hour analog dial — `OUTER_HOURS = [0..11]` (outerRadius=82), `INNER_HOURS = [12..23]` (innerRadius=50), computed via a shared `polar(index, count, radius, center)` trig helper. Plus a single-ring 12-position 5-minute-increment minute dial (`MINUTES = [0,5,...,55]`).
   - Clicking an hour commits it and switches `mode` to `'minute'`.
   - Clicking a minute commits and closes.
   - Click-outside-to-close via `document.addEventListener('mousedown', ...)` scoped to a `containerRef`.
   - This was the first anchored-popover pattern in the app (previously only full-screen modal dialogs existed).
- ARIA labels use `timeInput.hourAria: 'Hour {{value}}'` / `minuteAria: 'Minute {{value}}'` — must stay distinct from the picker header's bare hour/minute display text (an earlier bare `'{{value}}'` label caused an accessible-name collision in tests).

## UI redesign (2026-08-20 session)

A full visual/structural redesign was planned and executed via the superpowers brainstorming → writing-plans → subagent-driven-development pipeline. Source material: static HTML mockups in `roster-redesign.design/` (generated by an external design tool) using an Indigo/cool-slate "SaaS workbench" palette and Inter/Sora fonts — explicitly **not** adopted. Design doc: [docs/superpowers/specs/2026-08-20-ui-redesign-design.md](superpowers/specs/2026-08-20-ui-redesign-design.md). Implementation plan (16 tasks, full before/after diffs): [docs/superpowers/plans/2026-08-20-ui-redesign.md](superpowers/plans/2026-08-20-ui-redesign.md).

**Governing decisions (confirmed with user before planning):**
1. Borrow the mockups' layout/spacing/component structure only — re-expressed entirely in the *existing* coral/tan/sand/eucalyptus/ink palette and Fredoka/Noto Sans SC fonts. No Indigo, no new fonts.
2. Extract one consistent design system from the 5 mockup pages and apply it to all 16 real pages, not just the 5 with mockups.
3. Where a mockup implied functionality/fields that don't exist in the app (staff wizard's ID/gender/birthdate/nationality/social-security/address/department/grade/emergency-contact fields; roster page's week/month toggle and hover-swap) — keep the *interaction pattern* where sanctioned (the staff-creation wizard shell) but never add the underlying fields/features themselves.

**What shipped (15 commits on `master`, `d326707..905bc3f`, plus a docs commit `13e8fc8`):**
- `frontend/src/styles/ui.ts` gained shared table tokens — `tableShell`, `tableHeaderRow`, `tableHeaderCell`, `tableCell`, `tableRow` — and `cardBase`'s padding went `p-5`→`p-6`. Because `cardBase` is a shared constant already imported almost everywhere, that one change cascaded for free into pages that needed no other edit (HelpPage, RosterCreatePage, GroupDetailPage, etc.).
- `AppShell.tsx` / `PageHeader.tsx` / `AuthLayout.tsx` got spacing-only polish (sidebar/topbar padding, title tracking, form-panel spacing) — no structural or nav changes.
- New component `frontend/src/components/StatCard.tsx` (icon + value + label, accent-colored, wraps a `Link`) — wired into `DashboardPage.tsx`, which now fetches real counts via `api.staff/groups/shiftTemplates/rosters.list()` (all pre-existing endpoints) instead of showing static nav cards with no data.
- **Behavioral change (the one sanctioned in this redesign):** `StaffCreatePage.tsx` became a 2-step wizard — step 1 (name/email/responsibilities) → step 2 (`PreferenceFields`, unchanged). Same fields, same submit payload, only the flow changed. The responsibility-required validation moved from submit-time to the Step 1→2 "Next" transition.
- Every list-page table (Staff/Groups/ShiftTemplates/Responsibilities/Rosters, + Roster/Group detail tables where a matching literal existed) now uses the shared table tokens instead of duplicated inline Tailwind strings.
- New i18n keys added: `common.next`/`common.back`, `staff.step1Title`/`staff.stepIndicator` (en.ts/zh.ts).

**Two real bugs caught by the final whole-branch review (both fixed in `905bc3f`) — worth remembering as failure modes to re-check after future Dashboard/form changes:**
- `DashboardPage.tsx`'s four count-fetching `.then()` chains had no `.catch()`. `App.test.tsx` renders the real (unmocked) `DashboardPage`, so each fetch rejected with `ECONNREFUSED` as an *unhandled promise rejection* — Vitest printed "88 passed" but **exited code 1**. `npm test -- --run; echo $?` is the only way to catch this; the printed pass count alone is not sufficient evidence of a green suite. Fixed with `.catch(() => {})` per fetch.
- `StaffCreatePage.tsx`'s wizard step 1 was briefly a plain `<div>` (not `<form>`) with the Next button as `type="button"`, which silently disabled the browser's native `required`/`type="email"` validation (nothing else validates email format — the backend doesn't either). Fixed by making step 1 a real `<form onSubmit>` again with `type="submit"`.

**Known pre-existing gap, out of scope for the redesign:** `backend/src/staff/routes.ts` has no server-side email-format validation — only the frontend's native HTML5 validation catches malformed emails. A non-browser API caller could still write a bad email.

**Session's SDD (subagent-driven-development) mechanics**, for reuse next time a big multi-task plan needs executing:
- Ledger lived at `.superpowers/sdd/<plan-basename>/progress.md` (git-ignored scratch, deleted after the final review went clean — the commit history is the permanent record now).
- User explicitly declined an isolated git worktree for this plan ("直接在 master 上改") — all 16 tasks' commits landed straight on `master`. This is a valid path the skill supports (skip worktree, work in place) when the user says so.
- One task (RosterCreatePage) produced a legitimate **zero-diff** — its target patterns didn't exist in the file, and the plan's own instructions said "skip if not found, don't invent it." No task reviewer was dispatched for it since there was nothing to review; the controller verified the zero-diff claim directly via `git diff --stat` + grep instead.
- One reviewer finding was formally overruled as plan-mandated (Task 4: "single commit instead of split RED/GREEN commits" — the plan's own template specifies one commit).

**Current git state as of end of session:** all UI-redesign work is committed to local `master` but **not pushed** to `origin/master` (user chose to hold off; 16 commits ahead of remote as of this writing). Push whenever asked.

## Unavailable-dates feature (Group Management)

- `frontend/src/components/UnavailableDatesDialog.tsx` — dialog to set a staff member's unavailable date ranges from within Group Detail.
- Unavailable ranges now display directly on the member row in `frontend/src/pages/GroupDetailPage.tsx` (prefix + formatted ranges joined by `common.listSeparator`).
- **Bug fixed:** `GET /groups/:id/members` in `backend/src/groups/membershipRoutes.ts` used `include: { staff: true }`, which omitted the nested `staff.preference` relation. This silently wiped a member's `unavailableShifts`/`preferredShifts`/hours settings whenever saved via the Group Detail dialog (the dialog defaulted missing fields to blank/zero). Fixed to `include: { staff: { include: { preference: true } } }`. Verified live: set `unavailableShifts` via Staff Edit, made an unrelated save from Group Detail, confirmed data survived post-fix (was wiped pre-fix).

## Testing notes / gotchas

- All frontend test files were migrated from hardcoded Chinese assertions to English (the new default).
- As of 2026-08-20 end of session: Backend 129/129 passing (22 files). Frontend 88/88 passing (23 files — the 86/22 baseline plus `StatCard.test.tsx` and one added Dashboard-counts test). `tsc --noEmit` clean on both. **Always check the exit code, not just the printed pass count** — `npm test -- --run` can print "N passed" and still exit 1 on unhandled promise rejections (see the DashboardPage bug above); confirm with `npm test -- --run; echo $?`.
- For bulk Chinese→English string replacement in test files: read the file fully, then use a `python3 - <<'EOF'` heredoc via Bash with `(old, new)` pairs — terminal echo shows mojibake for CJK text due to console codepage, but the underlying UTF-8 file writes are correct. Always verify via `grep -n` on the actual file content afterward, not the terminal echo.
- Watch for loop-variable shadowing of `t()` (the translation function) — e.g. `.map((t) => ...)` on a templates array; rename the loop variable (e.g. `tpl`) to avoid confusion, even where JS scoping technically saves you.
- `frontend/src/__tests__/App.test.tsx` renders real, unmocked pages (including `DashboardPage`) through the actual router — any new top-level `useEffect` fetch added to a page reachable from `App.test.tsx`'s routes needs a `.catch()`, or it'll cause an unhandled-rejection exit-code failure invisible in the pass/fail count.

## Session norms / working agreements

- Only commit/push when the user explicitly asks (established from an earlier request: "今天先到这里，commit and push"; reconfirmed 2026-08-20 by choosing not to push the UI redesign's 16 commits yet).
- User communicates primarily in Chinese; feature requests should be translated into concrete implementation plans and verified both via automated tests and live browser checks before being considered done.
- When making UI changes, verify in-browser (not just type-check/tests) before reporting complete.
- For large multi-page/multi-file redesigns, the user is comfortable with the full brainstorm→spec→plan→subagent-driven-development pipeline (see "UI redesign" section above) rather than ad-hoc page-by-page edits — worth defaulting to for the next similarly large-scope UI request.
- The user's dev Postgres DB may carry small amounts of leftover test data (e.g. a `task6test` account, an `Alice` staff record) from manual verification passes during development sessions — harmless, but worth mentioning if counts/lists look unexpectedly non-empty in a fresh session.
